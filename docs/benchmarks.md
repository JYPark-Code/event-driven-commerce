# 부하테스트 결과 (Benchmarks)

> 측정한 것만 기록한다. 검증 안 된 수치 금지. (프로젝트 원칙)
> 각 측정마다: 환경 / 시나리오 / 수치(TPS·p50·p95·p99·에러율) / 병목 / 개선.

## 측정 환경 (공통)
- 머신: Intel i7-9700K (8C/8T), RAM 32GB, Windows 11 Pro
- 인프라: Docker Compose — MySQL 8.0(3307), Redis 7(16379), Kafka 3.8.1(KRaft 단일 노드)
- 앱: Java 17, Spring Boot 3.5.14, HikariCP max-pool=20, Tomcat maxThreads=200(기본값)
- 부하도구: k6 v2.0.0, **오픈 모델(constant-arrival-rate)** — 서버가 느려져도 도착률 유지 (coordinated omission 회피)
- ⚠️ **한계**: k6 + 앱 + Docker가 **같은 머신**에서 동작 → 절대 수치보다 상대 비교(동기 vs 비동기)가 목적

---

## 측정 1-a. 주문 동기 버전 기준선 (2026-06-10)

시나리오: `POST /api/orders` (단일 INSERT 트랜잭션), 도착률 고정 30초씩. 스크립트: `load-test/order-sync-baseline.js`

| 도착률 목표 | 실측 req/s | avg | p50 | p90 | p95 | max | 에러율 | drop |
|---|---|---|---|---|---|---|---|---|
| 100/s | 100.0 | 12.8ms | 12.1ms | 15.1ms | 17.0ms | 86ms | 0% | 0 |
| 500/s | 499.8 | 24.8ms | 16.2ms | 31.4ms | 59.8ms | 619ms | 0% | 0 |
| 1000/s (1차) | 989.0 | 161ms | 129ms | 355ms | 435ms | 2.0s | 0% | 0 |
| 1000/s (2차) | 986.1 | 224ms | 159ms | 526ms | 626ms | 1.9s | 0% | 68 |
| 1000/s (3차, 메트릭 샘플링 병행) | 903.4 | 1.13s | 1.17s | 1.53s | 1.63s | 2.8s | 0% | 1,497 |
| 1000/s (**콜드 스타트** 직후) | 771.5 | 2.23s | 2.12s | 2.91s | 3.14s | 4.6s | 0% | 5,321 |

### 결과 해석
- **1000 TPS 자체는 도달** (실측 986~989 req/s, 에러 0%). 단, p95가 17ms(100/s) → 435~626ms(1000/s)로 **약 30배 열화** — 처리량은 버티지만 큐잉이 심함.
- **콜드 스타트의 영향이 매우 큼**: JIT 워밍업 없이 풀부하 진입 시 p95 3.14s, 도착률 23% 미달(min조차 1.55s — SYN 재전송/accept 백로그 오버플로 패턴). → **측정 전 워밍업은 필수 절차**로 고정.
- **런 간 편차가 큼**(1000/s에서 p95 435ms~1.63s): 단일 머신에 부하기·앱·DB 동거 + 이전 런의 TIME_WAIT 소켓 누적 영향으로 추정. 비교 측정 시 동일 순서·동일 워밍업으로 통제할 것.

### 병목 분석 (측정 근거 기반)
| 증거 | 수치 | 해석 |
|---|---|---|
| CPU (1000/s 2차 중 샘플링) | 평균 14%, 최대 27% | **CPU 병목 아님** |
| HikariCP active/pending (3차 중 1초 간격 25회) | 전부 0 | **DB 커넥션 풀 병목 아님** — INSERT 자체는 sub-ms로 추정 |
| 서버측 누적 평균 (`http.server.requests`) vs 클라이언트측(3차) | 228ms vs 1.13s | 차이 ≈ 0.9s가 **서블릿 필터 밖**(Tomcat 큐·accept 백로그) 대기. 서버 내부 228ms도 DB 대기가 아니므로 스레드 스케줄링/락 의심 |
| 동시 in-flight ≈ 실측 900/s × 0.228s ≈ 205 | Tomcat maxThreads=200과 일치 | **워커 스레드 풀 포화**가 1차 병목 후보 |

- **미확정**: 워커 스레드가 DB 대기도 CPU도 아니면서 ~200ms를 소모하는 지점. 후보: Docker Desktop 포트 프록시(loopback→컨테이너) 경유 비용, GC, Hibernate 내부.
- **다음 실험**: (1) `server.tomcat.threads.max` 변화(100/200/400)로 p95 반응 확인, (2) `management.metrics.distribution.percentiles-histogram`으로 서버측 p95 직접 노출, (3) MySQL을 호스트 네이티브로 바꿔 Docker 프록시 영향 분리.

## 측정 1-b. 동기 vs Kafka 비동기 비교 (2026-06-10)

시나리오: `POST /api/orders/async` (orderKey 발급 → Kafka 발행 → 즉시 202, DB 저장은 컨슈머).
스크립트: `load-test/order-async.js`. 절차: 워밍업(100/s 30초, 기록 제외) 후 100 → 500 → 1000 → 1000 순서, 각 30초.
**드레인** = k6 종료 후 컨슈머가 백로그를 모두 DB에 반영할 때까지의 시간 (1초 간격 COUNT 폴링).

### 비동기 접수 경로
| 도착률 목표 | 실측 req/s | avg | p50 | p90 | p95 | max | 에러율 | drop | 드레인 |
|---|---|---|---|---|---|---|---|---|---|
| 100/s | 100.0 | 1.38ms | 1.05ms | 2.26ms | 3.31ms | 77ms | 0% | 0 | ~1s |
| 500/s | 500.0 | 0.81ms | 0.53ms | 1.38ms | 1.99ms | 14ms | 0% | 0 | 52s |
| 1000/s (1차) | 1000.0 | 0.82ms | 0.53ms | 1.08ms | 1.56ms | 141ms | 0% | 0 | 118s |
| 1000/s (2차) | 1000.0 | 0.69ms | 0.53ms | 1.06ms | 1.34ms | 59ms | 0% | 0 | 120s |

### 동기 교차 측정 (같은 앱 부팅에서, 비동기 런 직후)
| 도착률 목표 | 실측 req/s | avg | p50 | p95 | max | 에러율 | drop |
|---|---|---|---|---|---|---|---|
| 1000/s (1차) | 828.5 | 1.72s | 1.91s | 2.55s | 4.3s | 0% | 3,484 |
| 1000/s (2차, 90초 휴지 후) | 896.8 | 1.02s | 1.0s | 1.76s | 2.8s | 0% | 1,667 |

- 동기 교차 측정이 1-a(986~989/s, p95 435~626ms)보다 나쁨. 후보: orders 테이블 증가(7.5만→21만 행 + order_key 유니크 인덱스 추가), 같은 프로세스에 Kafka 컨슈머 동거, 직전 비동기 런의 소켓 잔여. **1-a에서 기록한 런 간 편차 범위(p95 0.4~1.6s)와 겹치므로 "동기는 1000/s에서 수백 ms~수 초 큐잉"이라는 결론은 유지.**

### 결과 비교 (1000/s 기준)
| 버전 | 실측 TPS | p50 | p95 | 에러율 | drop | 병목 |
|---|---|---|---|---|---|---|
| 동기 | 828~989 | 129ms~1.91s | 0.44~2.55s | 0% | 0~3,484 | Tomcat 워커 풀 포화 (요청 경로의 DB INSERT) |
| 비동기(Kafka) | **1000.0** | **0.53ms** | **1.34~1.56ms** | 0% | **0** | 접수 경로는 여유. **컨슈머 ~200건/s가 병목** |

### 결과 해석
- **가설 검증됨**: 요청 경로에서 DB INSERT를 빼자(브로커 ack 비대기 발행만 수행) p95가 수백 ms~초 → **1.3~1.6ms**로 떨어지고, 1000/s에서 도착률 100% 달성(drop 0). 동기가 in-flight ≈ 200(워커 풀 한계)에서 큐잉하는 동안, 비동기는 요청당 점유 시간이 sub-ms라 같은 워커 풀이 전혀 포화되지 않음.
- **단, 처리량 부채가 컨슈머로 이전됨**: 컨슈머 처리율 ≈ **183~202건/s** (파티션 3 × 컨슈머 3스레드). 1000/s × 30초 백로그(3만 건) 소진에 ~120초. "접수 1000 TPS"와 "처리 1000 TPS"는 별개 — 면접 포인트.
  - 컨슈머 이벤트당 ~15ms 소요(3스레드 × ~66건/s). 구성: existsByOrderKey SELECT + INSERT + 개별 트랜잭션 커밋(fsync) + Docker 포트 프록시 왕복.
  - **개선 후보(다음 실험)**: 배치 리스너 + JDBC batch INSERT(`rewriteBatchedStatements`는 이미 켜져 있음), 파티션·concurrency 증가, exists 검사 제거(유니크 제약 + 중복 무시로 SELECT 1회 절약).
- **정합성 확인**: 부하 후 orderKey 보유 주문 81,009건 = DISTINCT 81,009 = COMPLETED 81,009 (중복 0). 부하 중 DLQ 유입 0건 (DLQ 오프셋 1은 통합 테스트 주입분).
- **한계**: 비동기 202는 "브로커에 닿기 전" 유실 가능성을 남긴 측정(decisions.md 7번). 동기와 비동기는 응답이 보장하는 내용 자체가 다름(저장 완료 vs 접수 완료) — 수치 비교 시 이 전제를 같이 말해야 공정.

## 측정 1-c. 컨슈머 처리량 개선 실험 (2026-06-10)

1-b의 병목(컨슈머 ~200건/s)을 두 가설로 나눠 검증한다.
- 가설 A: 스레드 수 부족 → 파티션 12로 증설 후 concurrency 3/6/12 비교.
- 가설 B: 이벤트당 개별 트랜잭션(커밋 fsync + 왕복 3회)이 본질 → 배치 리스너 + JDBC batch INSERT.

절차는 1-b와 동일(워밍업 100/s 30초 → 1000/s 30초 + 드레인). **처리율 = 30,000건 ÷ (부하 30초 + 드레인)**.

### 1-c-i. 동시성 스케일 (파티션 12, 레코드 단위 컨슈머)
| concurrency | 드레인 | 컨슈머 처리율 | 배율(vs 3) | 접수 p95 |
|---|---|---|---|---|
| 3 (재기준) | 124s | ~195건/s | 1.0× | 1.87ms |
| 6 | 61s | ~330건/s | 1.7× | 2.62ms |
| 12 | 33s | ~476건/s | 2.4× | 4.68ms |

- 파티션 3→12 증설 자체는 영향 없음 (concurrency 3에서 1-b와 동일한 ~195건/s).
- 스레드 증가로 늘긴 하지만 **선형 미달**(12스레드에서 2.4×), 12스레드로도 1000/s에 한참 못 미침.
- 부작용: 컨슈머 스레드가 늘수록 **접수 경로 p95가 1.87 → 4.68ms로 열화** — 같은 프로세스/DB를 두고 경합.
- 결론: 가설 A 기각에 가까움. 이벤트당 고정 비용(개별 트랜잭션 커밋)이 지배적 → 가설 B로.

### 1-c-ii. 배치 리스너 + JDBC batch INSERT (concurrency 3)
폴링 배치 단위로 기처리 필터 SELECT 1회(IN) + multi-row `INSERT IGNORE` 1회 + 트랜잭션 커밋 1회.
(레코드 단위였을 땐 이벤트당 SELECT + INSERT + 커밋 = 왕복 3회 + fsync 1회)

| 도착률 | 실측 req/s | 접수 p95 | 드레인 | 컨슈머 처리율 |
|---|---|---|---|---|
| 1000/s | 1000.0 | 2.55ms | **0.7s** | **≥1000건/s (실시간 추종)** |
| 2000/s | 1999.9 | 1.60ms | **0.7s** | **≥2000건/s (실시간 추종, 한계 미도달)** |

- **195건/s → ≥2000건/s, 10배 이상 개선** (concurrency는 3 그대로 — 배치화 효과만 분리).
- 이벤트당 고정 비용(트랜잭션 커밋 fsync + 왕복 3회)을 배치(최대 500건, `max.poll.records` 기본값)로 상각한 효과. 가설 B 채택.
- 정합성: 누적 273,023건 = DISTINCT = COMPLETED (중복 0), 부하 중 DLQ 유입 0.
- 한 줄 결론: **동시성(스레드)을 늘리는 것보다 I/O 비용 구조를 바꾸는 것(배치화)이 한 자릿수 배 더 효과적이었다.**

### 측정 1 최종 상태 (1000/s 기준, 동일 머신)
| 버전 | 실측 TPS | 접수 p95 | 처리(저장) 완료 | 에러 |
|---|---|---|---|---|
| 동기 | 828~989 | 0.44~2.55s | 응답 시점에 완료 | 0% |
| 비동기 (레코드 컨슈머) | 1000 | 1.3~1.6ms | +120초 (컨슈머 랙) | 0% |
| 비동기 (배치 컨슈머) | 1000~2000 | 1.6~2.6ms | **+1초 미만** | 0% |

## 측정 2. 상품 조회 — 캐시 계층별 (2026-06-10)

시나리오: `GET /api/products/{id}`, 핫키 편중 읽기 — setup()에서 상품 100개 생성 후 요청의 80%는 상위 10개(핫키), 20%는 100개 균등. 스크립트: `load-test/product-read.js`.
절차: 1-a 교훈대로 **워밍업(100/s 30초, 기록 제외) → 1000/s 30초 본 측정**. 구성별로 앱을 새로 부팅(JVM 분리).
- **캐시 없음**: 커밋 `6237e30`(축 2 - 1단계, 캐시 코드 자체가 없음)을 worktree로 빌드해 측정.
- **L2만**: `ProductCacheLayer.get()`에서 L1을 우회하는 임시 빌드(커밋 안 함) — 매 요청이 Redis GET.
- **L1+L2**: 현재 코드 그대로.

### 결과 (1000/s, 워밍업 후 30초)
| 구성 | 실측 req/s* | avg | p50 | p90 | p95 | max | drop | 에러율 |
|---|---|---|---|---|---|---|---|---|
| 캐시 없음(MySQL 직접) | 942.7 | 82ms | 5.7ms | 273ms | **541ms** | 1.76s | 498 | 0% |
| L2(Redis)만 | 950.7 | 2.0ms | 1.0ms | 1.8ms | **3.8ms** | 167ms | 0 | 0% |
| L1(Caffeine)+L2(Redis) | 955.9 | **438µs** | <1ms | 518µs | **577µs** | 132ms | 0 | 0% |

\* k6의 req/s는 setup()(상품 100개 생성, ~2초)을 포함한 전체 시간으로 나눈 값이라 1000보다 낮게 표기됨. 본 구간 도착률은 1000/s이고, 캐시 구성은 drop 0으로 도착률을 전부 소화함. 캐시 없음만 drop 498(도착률 유지 실패).

참고 — **콜드 스타트**(부팅 직후 워밍업 없이 1000/s, 부트 직후 비교용):
| 구성 | 실측 req/s | avg | p95 | max | drop |
|---|---|---|---|---|---|
| 캐시 없음 | 899.2 | 237ms | 1.07s | 3.48s | 1,248 |
| L1+L2 | 941.9 | 7.7ms | 3.0ms | 1.16s | 0 |

### 계층별 히트 근거 (Redis `INFO stats` 카운터, 본 측정 30초 구간 델타)
| 구성 | keyspace_hits Δ | keyspace_misses Δ | 해석 |
|---|---|---|---|
| L2만 | +29,874 | +126 | 요청 30,100건 중 99.6%가 L2 히트. 미스 126 = 첫 로드 100 + **중복 DB 로드 26** — L1 우회로 single-flight가 없어 같은 핫키의 동시 첫 조회가 각자 DB로 간 흔적 (stampede의 축소판) |
| L1+L2 | **+0** | +100 | L2 GET 자체가 미스 100건(상품 100개의 첫 로드)뿐. 나머지 30,001건 전부 **L1에서 종결** — L1 TTL 60초 > 측정 30초라 본 측정 중 L1 만료가 없음 |

### 결과 해석
- **계층이 한 단계 올라갈 때마다 p95가 약 두 자릿수씩 준다**: DB 541ms → L2 3.8ms (~140×) → L1 577µs (~6.6×). 절대값보다 구조가 본질 — DB 경로는 1000/s에서 큐잉이 시작되지만(드롭 발생), L2는 Redis 왕복 1회(~1ms), L1은 프로세스 내 해시 조회라 네트워크가 아예 없다.
- **캐시 없음의 병목은 측정 1-a와 같은 패턴**: 에러 0%인데 p95 큐잉 + drop — 요청당 DB 왕복(SELECT)이 Tomcat 워커 점유 시간을 늘려 1000/s에서 워커 풀이 포화. 읽기 한 줄짜리 쿼리도 1000/s에선 못 버틴다는 게 측정으로 확인됨.
- **L1의 추가 이득은 "Redis 왕복 제거"**: L2만으로도 1000/s는 여유 있게 소화하므로(p95 3.8ms), L1의 가치는 평시 지연 ~3ms→0.6ms 단축과 **Redis 부하를 0으로** 만드는 것(히트 Δ=0). 인스턴스가 늘수록 Redis가 공유 병목이 되는 걸 L1이 차단한다 — 대신 인스턴스 간 불일치 창(L1 TTL 60초)을 감수 (decisions.md 12번).
- **L2만 구성에서 중복 DB 로드 26건 관측** — single-flight(L1의 Caffeine 로더)가 없을 때 동시 미스가 실제로 중복 로드를 만든다는 직접 증거. 본 구현(L1+L2)은 같은 구간에서 미스 = 정확히 키 수(100) — 키당 DB 로드 1회.
- **한계**: 같은 머신(k6+앱+Docker) 측정이라 절대값은 보수적으로 볼 것. 핫키 80/20은 인위적 분포 — 실제 트래픽의 지프 분포와는 다르나 "인기 상품 반복 조회"라는 캐시 전제를 표현하는 데는 충분.

## 측정 3. Tomcat 워커 스레드 튜닝 — 동기 주문 1000/s (2026-06-10)

측정 1-a에서 남긴 가설("워커 스레드 풀 포화가 1차 병목") 검증. `server.tomcat.threads.max` 100/200(기본)/400 비교.
절차: 구성별 **새 JVM 부팅 + orders TRUNCATE**(테이블 크기 변인 통제) → 워밍업 100/s 30초(기록 제외) → 1000/s 30초 × 2회. 스크립트: `order-sync-baseline.js`. 에러율은 전 구성 0%.

| threads.max | 런 | 실측 req/s | avg | p50 | p90 | p95 | max | drop |
|---|---|---|---|---|---|---|---|---|
| 100 | 1차 | 857.9 | 1.51s | 1.48s | 2.19s | 2.33s | 3.0s | 2,596 |
| 100 | 2차 | 911.1 | 836ms | 849ms | 1.35s | 1.43s | 2.1s | 1,270 |
| 200 (기본) | 1차 | 846.2 | 1.61s | 1.71s | 2.26s | 2.36s | 3.9s | 2,726 |
| 200 (기본) | 2차 | 904.5 | 1.09s | 1.18s | 1.56s | 1.69s | 2.7s | 1,507 |
| 400 | 1차 | 914.5 | 1.16s | 1.14s | 1.76s | 2.06s | 5.5s | 1,233 |
| 400 | 2차 | 962.3 | 653ms | 592ms | 1.19s | 1.46s | 4.4s | 400 |

### 결과 해석
- **스레드 수는 결정 변수가 아니었다**: 100→400에서 p95가 전부 1.4~2.4s 밴드에 겹친다. 400이 도착률·drop에서 약간 우세하지만, **모든 구성에서 2차 런이 1차보다 일관되게 좋아서**(JIT·페이지 캐시 추가 워밍업) 런 순서 효과가 구성 효과와 같은 자릿수다.
- **측정 1-a의 "워커 풀 포화(in-flight ≈ 200 = maxThreads)" 가설은 기각에 가깝다**: 풀을 절반(100)으로 줄여도 유의하게 나빠지지 않고, 2배(400)로 늘려도 큐잉이 사라지지 않는다. 오늘 수치로 Little's law를 다시 쓰면 in-flight ≈ 900/s × 1.1s ≈ **1,000** — 어떤 풀 크기도 초과한다. 병목은 워커 "수"가 아니라 **부하 시 요청당 서비스 시간 자체의 증가**(트랜잭션 커밋 fsync + 같은 머신의 k6·Docker CPU 경합)로 해석을 수정한다.
- **일 단위 환경 편차가 튜닝 효과보다 크다**: 오늘 세션 전체(p95 1.4~2.4s)가 1-a 측정일(0.44~0.63s)보다 일관되게 나쁨. 같은 머신 측정에서는 **다른 날짜의 수치를 직접 비교하면 안 되고**, 비교는 같은 세션 안에서만 유효하다는 한계 재확인.
- **결론: `threads.max` 기본값 200 유지** — 수치가 정당화하지 않는 튜닝 값을 설정에 박지 않는다 (decisions.md 15번).

## 측정 4. 종합 혼합 부하 — 조회 80% + 비동기 주문 20% (2026-06-10, 축 4 최종)

단일 엔드포인트 측정(1~3)과 달리 **실서비스形 트래픽 믹스**로 1000 TPS 목표를 최종 확인하고 한계를 탐색한다.
시나리오: `mixed-final.js` — `GET /api/products/{id}`(핫키 80/20) 80% + `POST /api/orders/async` 20%를 **동시에** (k6 시나리오 2개 병행).
절차: 단일 부팅(기본 설정: threads 200, Hikari 20, 배치 컨슈머) → 혼합 워밍업 100/s 30초(기록 제외) → 1000 → 2000 → 3000 → 5000/s 각 30초. 드레인 = k6 종료 후 orders COUNT 1초 폴링 안정화까지.

| 합산 도착률 | 실측 req/s* | 조회 p95 | 주문 p95 | 전체 p50 | 에러율 | drop | 드레인 |
|---|---|---|---|---|---|---|---|
| **1000/s** (800+200) | 962.7 | **3.1ms** | **3.5ms** | 0.5ms | 0% | 0 | ≤2s |
| 2000/s (1600+400) | 1,916.0 | 1.6ms | 1.6ms | 0.5ms | 0% | 0 | ≤2s |
| 3000/s (2400+600) | 2,884.4 | 3.1ms | 3.2ms | 0.5ms | 0% | 0 | ≤2s |
| 5000/s (4000+1000) | 4,732.1 | 17.7ms | 20.1ms | 0.7ms | **2.4%** | 2,220 (전부 조회) | ≤2s |

\* k6 req/s는 setup(상품 100개 생성)을 포함한 전체 시간으로 나눈 값 (측정 2와 동일 표기). 3000/s까지는 drop 0 — 도착률 전부 소화.

### 5000/s 실패 분해
- 주문 202: **30,001 / 30,001 통과 (무손실)** — 한계 구간에서도 주문 접수는 깨지지 않음.
- 실패 3,515건은 **전부 조회**(read 200 실패율 3.0%) + 조회만 drop 2,220 — 한계 진입은 트래픽의 80%를 차지하는 조회부터.
- 정합성(세션 누적): orderKey 보유 66,604 = DISTINCT 66,604 = COMPLETED 66,604 — **중복 0, 미처리 0**.

### 결과 해석
- **1000 TPS 목표 달성**: 혼합 트래픽 1000/s에서 에러 0%, drop 0, p95 3.5ms 이하. 주문 "처리(저장)"까지 실시간 추종(드레인 ≤2초) — 접수만 1000이 아니라 처리도 1000건/s.
- **한계(무릎)는 3000~5000/s 사이**: 3000/s까지 p95 한 자릿수 ms 유지, 5000/s 목표에서 실측 4,732/s + 에러 2.4%.
- **4개 축의 종합 결론**: 같은 머신에서 동기 단일 경로는 ~900/s에서 초 단위 큐잉(측정 3), 혼합 경로는 3000/s까지 ms 단위 — 차이의 원인은 **요청 경로에 DB가 없다는 것**(주문 = Kafka 발행 후 202, 조회 = L1 히트). 1000 TPS를 만든 건 풀 튜닝이 아니라 경로 설계였다.
- **한계**: k6·앱·Docker 동거 측정 — 5000/s에서는 k6 자체의 CPU 경합이 커져 한계 수치는 오염 가능(보수적으로 볼 것). 조회 실패 3%의 원인 분리(서버 accept 큐 vs 부하기 소켓 고갈)는 부하기를 분리해야 가능 — 데모 범위 밖으로 남긴다.

## 측정 5. [msa 브랜치] 게이트웨이 경유 혼합 부하 — 조합 앱 vs MSA 구성 (2026-06-12)

decisions 22번에서 범위 밖으로 미뤘던 MSA 구성의 베이스라인. **측정 3의 교훈(일 단위 환경 편차 > 튜닝 효과)에 따라 측정 4(다른 날짜)와는 비교하지 않고, 같은 세션 안에서 두 구성을 연달아 측정해 게이트웨이 홉 + 프로세스 분리 비용만 분리한다.**

- 구성 A: 조합 앱 단독(:8080, 모놀리스 형태) — 측정 4와 같은 실행 형태.
- 구성 B: gateway-app(:8080) + product/order/backoffice 분리 앱 — 전 트래픽이 게이트웨이 경유. 인증 라우트 rate limit(decisions 24)은 측정 경로(`/api/products`·`/api/orders`)와 무관.
- 절차: 구성별 새 JVM(jar 직접 실행) → 혼합 워밍업 100/s 30초(기록 제외) → 1000 → 2000 → 3000 → 5000/s 각 30초. 드레인 = orders COUNT 1초 폴링 안정화. k6 v2.0.0, 시나리오 `mixed-final.js`(측정 4와 동일). B는 1차 런 편차 확인용으로 2000·1000을 2차 재측정.
- 보안 하드닝(decisions 23~25) 적용 상태의 측정: Flyway `validate`, 내부 API 토큰, 관리 포트 분리(스크레이프 9080).

### 구성 A — 조합 앱 (세션 전반부)
| 합산 도착률 | 실측 req/s | 조회 p95 | 주문 p95 | 에러율 | drop | 드레인 |
|---|---|---|---|---|---|---|
| 1000/s | 971.3 | 5.06ms | 4.70ms | 0% | 0 | ≤1.4s |
| 2000/s | 1,922.1 | 8.51ms | 7.45ms | 0% | 579 | ≤1.7s |
| 3000/s | 2,920.2 | 1.99ms | 2.00ms | 0.29% | 0 | ≤1.4s |
| 5000/s | 4,764.6 | 21.1ms | 22.0ms | 1.39% | 2,614 | ≤1.4s |

### 구성 B — 게이트웨이 + 분리 앱 4개 (세션 후반부)
| 합산 도착률 | 실측 req/s | 조회 p95 | 주문 p95 | 에러율 | drop | 드레인 |
|---|---|---|---|---|---|---|
| 1000/s (1차) | 963.5 | 13.2ms | 9.08ms | 0.01% | 0 | ≤1.4s |
| 2000/s (1차) | 1,892.5 | 37.0ms | 61.5ms | **5.61%** | 927 | ≤1.4s |
| 3000/s | 2,894.3 | 4.09ms | 4.13ms | 0% | 217 | ≤1.4s |
| 5000/s | 4,364.8 | 254ms | 330ms | **12.1%** | 15,364 | ≤1.4s |
| 2000/s (2차) | 1,942.4 | **3.29ms** | **3.24ms** | 0% | 0 | ≤1.4s |
| 1000/s (2차) | 966.9 | **2.76ms** | **2.83ms** | 0% | 0 | ≤1.4s |

### 결과 해석
- **1000 TPS 목표는 MSA 구성에서도 달성**: 워밍업된 2차 런 기준 에러 0%, drop 0, p95 2.8ms. 2000/s 2차도 에러 0%·p95 3.3ms — 평시 영역에선 게이트웨이가 병목이 아니다.
- **게이트웨이 홉 + 프로세스 분리 비용 ≈ 1ms 안팎**: 두 구성의 워밍업된 런을 비교하면(A의 3000런 p95 2.0ms ↔ B의 2차 런 2.8~3.3ms) 차이가 +0.8~1.3ms로, 같은 세션의 런 간 편차와 같은 자릿수다. "홉이 비싸다"가 아니라 "홉 비용은 편차에 묻히는 수준"이 정확한 결론.
- **1차 런 편차가 MSA 구성에서 훨씬 크다**: B의 2000/s 1차는 에러 5.6%였는데 같은 조건 2차는 0%·p95 3.3ms — 용량 한계가 아니라 워밍업 부족이었다(직후의 3000/s가 에러 0%인 것이 방증). 프로세스가 4개라 JIT·커넥션 풀(k6→게이트웨이, 게이트웨이→백엔드 2단)의 워밍업 표면이 그만큼 넓다. **다단 구성의 측정은 워밍업을 구성 단수에 비례해 늘려야 한다**는 절차 교훈.
- **고부하 한계는 MSA 구성이 뚜렷이 낮다**: 5000/s에서 A는 에러 1.4%·p95 22ms, B는 에러 12%·p95 254~330ms·drop 15,364. 같은 머신에 JVM 5개(+k6)가 동거하며 CPU를 나누고, 홉마다 소켓·컨텍스트 스위치가 2배가 되는 구조적 비용이 한계 구간에서 증폭된다. 원인 분해(게이트웨이 자체 vs CPU 경합)는 부하기·서비스를 머신 분리해야 가능 — 범위 밖.
- **정합성·드레인은 구성과 무관하게 유지**: 매 런 종료 후 orders COUNT 증가분 = 202 응답 수 정확 일치(접수된 주문 무손실 — 5000/s 한계 구간 포함), 세션 누적 147,984 = DISTINCT = COMPLETED(중복 0). 드레인 전 구간 ≤1.7초. 부팅 시 토픽 재생 백로그 66,568건도 ~13초에 소진(배치 컨슈머).
- **한계**: 단일 머신 측정이라 B의 고부하 열화에는 "프로세스 4개의 CPU 경합"이 섞여 있다 — 운영형 배치(서비스별 머신)라면 한계는 더 높고 홉 비용은 네트워크 RTT만큼 더 커진다. 절대값이 아니라 "평시 홉 비용 ≈ 편차 수준, 한계는 자원 경합이 결정"이라는 구조적 결론만 가져갈 것.
